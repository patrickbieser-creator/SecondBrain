# Copy to .env and fill in your keys
# The app stores keys in electron-store (encrypted on disk).
# These env vars are read at startup if electron-store keys are empty.

OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
SECONDBRAIN_URL=http://localhost:3000
